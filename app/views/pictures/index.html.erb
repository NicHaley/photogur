<h1>Pictures</h1>
<%= link_to "New Picture", "/pictures/new" %>

<% @pictures.each do |picture| %>
	<p><%= picture.title %>, <%= picture.artist %></p>
	<%= link_to image_tag(picture.url), "/pictures/#{picture.id}" %>   <!-- link_to and image_tag are 'helpers', ie.special Ruby on Rails functions
<% end %>